/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['firebasestorage.googleapis.com', 'rpmzykoxqnbozgdoqbpc.supabase.co']
    }
};

export default nextConfig;
